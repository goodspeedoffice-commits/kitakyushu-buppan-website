# 引き継ぎ：組合公式サイト Wix → Cloudflare Pages 移行（2026-08-20 Claude → Codex）

このファイルの内容をそのまま Codex に渡せば作業を継続できる。

## 現在地（2026-08-20 更新）

| タスク | 状態 |
|---|---|
| A. アイコン整列の修正 + Brevo 移行のデプロイ | ✅ 完了・本番反映済み |
| B. 組合員専用ページを Cloudflare Access で新設 | ⬜ 未着手（C の前提条件） |
| C. ドメインを新サイトへ切替 | ⬜ 未着手 |
| D. `wrangler.toml` の `preview_urls` 無効化 | ⬜ 未着手 |

**次にやること: B → C → D。**

---

## 0. 引き継ぎ先プロジェクト／リポジトリ

- **Codex プロジェクト: `北九州物販事業者協同組合 2`**
  （`北九州物販事業者協同組合`（無印）は 2026-05-16 の議事録 1 件のみの旧プロジェクト。**そちらではない**）
  組合 HP 関連のセッション 33 件はすべて `北九州物販事業者協同組合 2` に蓄積されている。

### 0-1. リポジトリ

- パス: `C:\Users\goods\cloude\北九州物販事業者協同組合\組合ホームページ`
- ローカルブランチ: **master**
- Cloudflare Pages 側の本番ブランチ: **master**（2026-08-20 に Codex が `main` から揃えた／コミット `3ef32c5`）
- デプロイは `npx wrangler pages deploy`（**`--branch main` は不要になった**。旧手順に注意）

---

## 1. 今日判明した最重要事実

CEO が見ている本番 `www.kitakyusyubuppan.com` は **今も Wix**。
Claude / Codex が編集してきたのは `kitakyushu-buppan.pages.dev`（Cloudflare Pages）で、**別サイト**。

このため「指摘した箇所を直したはずなのに直っていない」という状態が続いていた。
`DEPLOY.md` にも「独自ドメインは**未接続**。現在は Wix を指している」と記載がある。

---

## 2. CEO の指摘と原因（ブラウザ実測で特定済み）

### 指摘① 「事務所紹介のテキスト文章が消えている」

Wix トップページ `y≈1714` の見出し「事務所紹介」の文字色が **白 `#FFFFFF`**（`span.color_36`）。
一方、背後の祖先要素は `SECTION` まで遡ってすべて `background-color: rgba(0,0,0,0)` で **背景画像なし**。
→ **白背景の上に白文字**が乗っており見えない。

対照：隣の「参加企業紹介」見出しは `#00305B`（紺）で正常に見えている。
以前この帯にあった背景画像を外した際、文字色が白のまま取り残されたと推定される。

### 指摘② 「参加企業紹介のアイコン類の位置合わせがバラバラ」

アイコン中心の X 座標（実測値）：

| | 1列目 | 2列目 | 3列目 | 4列目 |
|---|---|---|---|---|
| 1行目 (y=3361) | 144 | 380 | 607 | 837 |
| 2行目 (y=3902) | 171 | 396 | 637 | 870 |
| 列ズレ | 27px | 16px | 30px | 33px |

- 横の間隔：1行目 236 / 227 / 230px、2行目 225 / 241 / 233px（等間隔でない）
- 社名テキストのベースライン：3602 / 3605 / 3607 / 3608（最大 6px ズレ）
- 「北九州物販事業者協同組合」だけ社名が 2 行に折り返し、代表者名が 20px 下にズレている
- SNS アイコン（39px）も goodspeed 下は cx=71/120/169/218 と左寄せで、カード中心 144 とズレている

原因：Wix エディタでドラッグ配置され、整列機能が使われていない。

---

## 3. CEO の決定（2026-08-20）

1. **新サイトにドメインを切り替える**（Wix の手直しはしない）
2. **組合員専用ページは Cloudflare Access で作り直す**
3. デプロイは `contact.js` の状態を確認してから行う

---

## 4. Claude が本日実施した変更【コミット済み・本番反映済み】

新サイト側にも本物の欠陥があったため修正した。

**欠陥の内容**：アイコンに絵文字を使っており、書体ごとに字幅が違うため揃わなかった。
実測グリフ幅 → `▶` 12.48px / `f` 4.06px / 絵文字 17.14px。
結果、バッジ幅が 86.4 / 99.5 / 84.3px と不揃いだった。

**変更点**

- 絵文字アイコンをすべて**インライン SVG + 固定幅スロット**へ置換
  - `members.html`（公式サイト / YouTube / Instagram / Facebook）
  - `about.html`（所在地・電話・メール・受付時間・地図の 5 アイコン、SNS ボタン 3 個）
  - `contact.html`（所在地・電話・メール・受付時間の 4 アイコン）
  - 全 HTML のフッター SNS リンク 3 個
- `.member-card` を `display:flex; flex-direction:column; align-items:center` 化
- `.member-card .member-links` に `margin-top:auto` → リンク行がカード下端で揃う（リンクなしカードとも高さ一致）
- `.member-card h3` に `line-height:1.5; min-height:3em` → 社名が 2 行でも下の要素が動かない
- 既存の `.contact-item .icon` と新規追加の `.icon` が**クラス名衝突**していたため、新規側を `.svg-icon` にリネーム（衝突したままだと contact.html の絵文字が 16×16 の block に潰れる）
- `.info-row` / `.info-icon` / `.sns-buttons` / `.sns-btn` を新設し、`about.html` のインライン style を置き換え
- 全 HTML の `?v=20260820` → `?v=20260820b`（CSS 変更のキャッシュバスター）

**検証結果（ローカル実測・すべて合格）**

- `.member-link` のアイコン幅 **14px 統一**、上下中心ズレ **0**、バッジ高さ **30px 統一**
- カード内の要素位置：avatar 29px / h3 109px / rep 161px / links 201px → **全カード一致**
- `about.html` の `.info-row`：アイコン枠 **24px 統一**、ラベル開始位置 **38px 統一**、SVG 21×21 統一
- `contact.html`：アイコン枠 **22px 統一**、テキスト開始 **34px 統一**
- 幅ゼロの SVG **なし**

**差分規模**：`public/` 配下 16 ファイル + `css/style.css`

**反映状況**：Codex がコミット `86592cd`「サイトの見た目を調整（別セッションによる変更を取り込み）」として取り込み、
本番へデプロイ済み。`https://kitakyushu-buppan.pages.dev/members` `/about` `/contact` で
SVG アイコンの配信を確認済み（絵文字アイコンの残存なし）。

---

## 5. 未完了タスク

### A. デプロイ前の確認 ―― ✅ 完了（2026-08-20）

`functions/api/contact.js` の Resend → Brevo 移行は Codex がコミット `d9a5e5a`
「お問い合わせのメール通知を Brevo で有効化」として確定し、本番へデプロイ済み。
Claude のアイコン整列修正（`86592cd`）も同時に反映されている。**このタスクは対応不要。**

### B. 組合員専用ページの新設【未着手・ドメイン切替の前提条件】

現在 `public/_redirects` に以下があり、このまま切り替えると **Wix の組合員専用ページが消える**。

```
/blank    /    301
```

- Cloudflare Access で**特定メールアドレスのみ許可**するページを新設する
- 独自パスワード認証・PIN ハッシュの JS 埋め込みは社内ルールで**禁止**
- 組合員メールアドレスの正本：「組合用スプレッド一覧」の『ホームページ・メールアドレス管理』タブ ＋ 1Password
- 新設後、`_redirects` の `/blank` を新ページへ向け直す

#### B-1. 未完了リクエストを取り込むこと（2026-08-14・Codex セッション `019ffcbb`）

CEO から以下の依頼が出ていたが、**Wix 側で実現できず未完了のまま止まっている**。
新サイトで作り直す際に、この要件を組合員専用ページへ統合すること。

> 組合員専用ページに**サイトマップ（リンク集）ページ**を新設したい。
> 商品ページのリンクをサイト内に独立した形で置き、Instagram・Facebook・YouTube 動画などの
> 外部リンクもまとめる。**後で組合員全員に配布する**ための土台ページにしたい。

その時の Codex 自身の結論が、今回の方針の裏付けになっている：

> 「**Wix の管理 API では通常ページのレイアウト作成までは扱えない**ため、既存 Wix エディタの状態を確認して
> ページを追加します」→ ブラウザ操作でも Wix 画面が応答せず **作成は未完了**。サイトは未変更。

つまり **Wix 上ではこの要件を自動化できない**。新サイト（Cloudflare Pages）ならコードで作れるため、
ドメイン切替と同時に解決できる。

**関連セッション（`北九州物販事業者協同組合 2` プロジェクト内）**
- `2026-08-14` 組合員専用ページにサイトマップ／リンク集を新設 → **未完了**
- `2026-08-13` 参加企業紹介欄の空白事業者にサンプルアイコンを設定
- `2026-08-13` 参加企業紹介ページの管理方法と該当企業の HP 欄 URL 更新
- `2026-08-10` 組合の HP が作りっぱなしで放置（本件の発端）

### C. ドメイン切替【未着手】

現状の DNS：

- NS: `ns14.wixdns.net` / `ns15.wixdns.net`（Wix に完全委任）
- `www.kitakyusyubuppan.com` → CNAME `cdn1.wixdns.net` → `34.149.87.45`

| 方法 | 作業 | 備考 |
|---|---|---|
| **A（推奨・影響小）** | Wix 管理画面で DNS レコードを編集し `www` の CNAME を `kitakyushu-buppan.pages.dev` へ。Cloudflare Pages 側でカスタムドメインを追加 | NS は Wix のまま。他レコードに影響しない |
| B | レジストラで NS を Cloudflare へ変更 | メール等の他レコードも移設が必要。作業が重い |

**B（組合員専用ページ）を完了させてから切り替えること。**

旧 Wix URL の引き継ぎは `public/_redirects` に設定済み（`/about-5` `/general-5` `/map` `/blog` `/blog/*` `/post/*` `/複製-*` `/pages-sitemap.xml` 等）。**未解決は `/blank` のみ**。

### D. 社内ルール違反の是正【未着手】

`wrangler.toml` に **`preview_urls` の記載がない**（2026-08-20 時点で 0 件・未対応）。
社内ルールでは「Cloudflare Workers の `preview_urls` を有効のままにしない」と定めているため、明示的に無効化すること。

---

## 6. 作業時の注意

- CSS / JS を更新したら全 HTML の `?v=YYYYMMDD` を更新する（`DEPLOY.md` 記載）
- フッターの Amazon 非提携の免責文は**維持する**（Amazon 広告パートナーネットワーク申請対応のため）
- サイト文言に「Amazon 公認」等、誤解を招く表現を入れない
- `.icon` というクラス名は `.contact-item .icon` と衝突するので使わない。SVG アイコンは `.svg-icon` を使う

---

## 7. 参照

- `DEPLOY.md` — デプロイ手順・D1 マイグレーション・Secrets 登録
- 新サイト本番: https://kitakyushu-buppan.pages.dev
- Wix 現行: https://www.kitakyusyubuppan.com
